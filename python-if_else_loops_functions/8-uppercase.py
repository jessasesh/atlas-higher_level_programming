#!/usr/bin/python3
def uppercase(str):
    for letter in str:
        ucase = (ord(letter) 
                if ucase in range(97, 123):
                    ucase = ucase - 32

                print("{:c}".format(ucase), end="")
    print()
