#!/usr/bin/node
// Define an array of strings
const sayWhat = ["C is fun", "Python is cool", "JavaScript is amazing"];

// Initialize a loop counter
let i = 0;

// Loop through the array and print each string
for (i = 0; i < sayWhat.length; i++) {
    console.log(sayWhat[i]);
}
